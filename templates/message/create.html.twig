{% extends 'base.html.twig' %}

{% block title %}Nicomak | Dire Merci{% endblock %}

{% block body %}
<style>
    .main-content { 
        margin: 1em auto; 
        max-width: 800px; 
        width: 95%; 
        font: 18px/1.5 sans-serif; 
    }
    .main-content code { 
        background: #F5F5F5; 
        padding: 2px 6px; 
    }

    label {
        display: block; 
        margin-bottom: 0.5em; 
        font-weight: bold; 
    }

    input, textarea {
        width: 100%; 
        padding: 0.5em; 
        margin-bottom: 1em; 
        border: 1px solid #ccc;
        border-radius: 4px; 
        font-size: 16px; 
    }

     input, select {
        width: 10%; 
        padding: 0.5em; 
        margin-bottom: 1em; 
        border: 1px solid #ccc;
        border-radius: 4px; 
        font-size: 16px; 
    }

    .btn {
        background-color: #007bff; 
        color: white; 
        border: none; 
        padding: 0.75em 1.5em; 
        border-radius: 4px; 
        cursor: pointer; 
    }
    .btn:hover {
        background-color: #0056b3;
    }
</style>
<div class="main-content">
    <h1>Dire Merci</h1>
    <p><i>c'est reconnaître que chaque geste, chaque attention reçue, 
    est un cadeau précieux qui ne doit jamais être pris pour acquis.</i></p>


    {{ form_start(form) }}
        {{ form_row(form.message) }}  
        {{ form_row(form.createdAt) }}  
        {{ form_row(form.fromUser) }}   
        {{ form_row(form.toUser) }} 
        <button class="btn">Envoyer</button>
    {{ form_end(form) }}

    <br><br>
    
    <a href="{{ path('app_message') }}">Retour à la liste des messages</a>
</div>
{% endblock %}

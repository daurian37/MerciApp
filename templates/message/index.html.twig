{% extends 'base.html.twig' %}

{% block title %}Hello MessageController!{% endblock %}

{% block body %}
<style>
    .main-content { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .main-content code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="main-content">
    <h1>Bienvenue sur le site Nicomak</h1>
    <p>A Nicomak, on aime bien remercier les collègues qui nous ont donné un coup de main.</p>
    <p>
        <a href="{{ path('create_message') }}">Cliquez ici pour remercier un collègue</a>
    </p>

    {% if messages %}
        <ul class="flashes">
        {% for message in messages %}
            <li>
                {{ message.message }} <br />
                <span><i>Publié le: {{ message.createdAt|date('d/m/Y H:i:s') }} 
                par {{ message.fromUser }}  à {{ message.toUser }}</i></span>
            </li>
        {% endfor %}
        </ul>
    {% else %}
        <p>Aucun message trouvé.</p>
    {% endif %}
    
</div>
{% endblock %}
